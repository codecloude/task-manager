import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
    title: "Рабочее пространство | ilyask",
};

const Workspace = () => {
    return <>
        
    </>;
};

export default Workspace;
